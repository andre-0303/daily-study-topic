services:
  backend:
    build:
      context: ./backend
    container_name: study-topics-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: study-topics-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend